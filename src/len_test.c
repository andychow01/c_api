#include <cstdio>
#include <time.h>

int main() {
    printf("len of int: %d\n", sizeof(int));
    printf("len of long: %d\n", sizeof(long));
    printf("len of time_t: %d\n", sizeof(time_t));
    printf("len of size_t: %d\n", sizeof(size_t));
}
